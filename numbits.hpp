
/**
 *function to count the number
 * of 1 bits in the int parameter
 * and return the result
 *@param i the int in question
 *@return as above
 **/
unsigned int num1bits(int i);


/**
 *function to count the number
 * of 0 bits in the int parameter
 * and return the result
 *@param i the int in question
 *@return as above
 **/
unsigned int num0bits(int i);